// import Zyp from '../../static/zyphub/dist/zyphublib.js'
// var zyp = new Zyp()

export const fetchMessages = function ({ dispatch }) {
  // Call the messages service on the server via websocket
  console.log('fetching')
  // zyp.getMessageList()
  // dispatch('FETCH_MESSAGES')
}

// export const addMessage = ({ dispatch }) => {
//   dispatch('ADD_MESSAGE')
// }

// export const editMessage = ({ dispatch }, e) => {
//   dispatch('EDIT_MESSAGE', e.target.value)
// }

// export const deleteMessage = ({ dispatch }) => {
//   dispatch('DELETE_MESSAGE')
// }

// export const updateActiveMessage = ({ dispatch }, message) => {
//   dispatch('SET_ACTIVE_MESSAGE', message)
// }
